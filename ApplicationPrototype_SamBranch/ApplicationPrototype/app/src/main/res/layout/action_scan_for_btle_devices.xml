<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:layout_marginTop="@dimen/action_bar_vertical_height">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/bt_device_name"
        android:text="BT Device Name"
        android:textColor="@color/DarkBlueGray"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginTop="5dp"
        android:layout_centerHorizontal="true"/>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_below="@id/bt_device_name"
        android:layout_centerHorizontal="true"
        android:id="@+id/btn_scan_linear_layout">
        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/bt_search"
            android:drawableLeft="@drawable/ic_action_search"
            android:text="Scan For Devices"
            android:backgroundTint="@color/DarkBlueGray"
            />
        </LinearLayout>
        <ListView
            android:outlineAmbientShadowColor="@color/DarkBlueGray"
            android:id="@+id/bt_device_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/btn_scan_linear_layout"
            android:layout_marginTop="0dp"
            android:layout_marginBottom="5sp" />

</RelativeLayout>